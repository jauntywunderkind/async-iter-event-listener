# async-iter-event-target

> Get an asynchronous iterator for a stream of EventTarget/EventEmitter events

# node.js note

if you just want to read data from streams in node.js, they now implement AsyncIteration.
